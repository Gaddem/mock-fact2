# Fins de ligne normalisées en LF dans le dépôt, quel que soit le poste.
# Sans ça, un aller-retour Windows / macOS produit des diffs de fichiers entiers.
* text=auto eol=lf

# Binaires : ne jamais toucher
*.jpg binary
*.png binary
*.ico binary
*.woff2 binary
